import { HighlightVotrDirective } from './highlight-votr.directive';

describe('HighlightVotrDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightVotrDirective();
    expect(directive).toBeTruthy();
  });
});
